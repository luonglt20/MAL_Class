[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "malware-hybrid-attention"
version = "0.2.0"
description = "Independent multimodal attention for multi-label malware-family classification"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.23",
  "PyYAML>=6.0",
  "torch>=2.1",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
dashboard = ["streamlit>=1.24", "pandas>=1.5", "plotly>=5.0"]
fuzzy = ["py-tlsh>=4.12", "ppdeep>=20200505"]

[project.scripts]
malware-hybrid = "malware_hybrid.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
