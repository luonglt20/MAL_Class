# MAL_Class — independent Hybrid Attention pipeline
numpy>=1.23
PyYAML>=6.0
torch>=2.1

# Dashboard and graph visualisation
streamlit>=1.24
pandas>=1.5
plotly>=5.0
networkx>=3.0

# Optional fuzzy-hash backends are intentionally not mandatory:
#   pip install -e '.[fuzzy]'
